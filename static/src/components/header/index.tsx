import { h } from "preact";

const Header = () => (
    <header className="bg-sky-400 fixed left-0 top-0 w-full h-16 shadow-float flex flex-row items-center z-50">
        <h1 className="grow-0 px-4 text-xl text-white">QQ Lite</h1>
    </header>
);

export default Header;
